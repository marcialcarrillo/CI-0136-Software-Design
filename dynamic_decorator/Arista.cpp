#pragma once
#include "Arista.h"

Arista::Arista(Nodo * first, Nodo * second) {
	this->first = first;
	this->second = second;
}

//Arista::Arista(Nodo* first, Nodo* second) : first { first } *second { second }
//{
//}